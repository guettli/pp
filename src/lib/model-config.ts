/**
 * Single place to configure the ZIPA model used throughout the project.
 * Update MODEL_NAME here to switch to a different model everywhere.
 */
export const MODEL_NAME = "zipa-small-crctc-ns-700k";
export const HF_REPO = `anyspeech/${MODEL_NAME}`;
export const MODEL_FILE = "model.fp16.onnx";
